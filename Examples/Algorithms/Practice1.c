#include <stdio.h>

int main()
{
    int balance = 0;
    int holder = 0;

    printf("Please input CC balance");
    scanf("%d", &balance);
    if(balance <= 100)
    {
        balance * .08 = holder;
        balance += holder;
    }
    else if(balance > 500)
    {

    }
    else
    {

    }
}